# Server Configuration
PORT=10000
NODE_ENV=development

# Database Configuration (Replace with your MongoDB URI)
MONGODB_URI=mongodb+srv://<username>:<password>@<cluster>.mongodb.net/<database>

# JWT Configuration (Use a secure secret in production)
JWT_SECRET=your_jwt_secret_here

# CORS Configuration (Frontend URL)
CORS_ORIGIN=http://localhost:5173

# Note: In production (Render), these values are set in the dashboard
# and the PORT is automatically set to 10000 as specified in render.yaml